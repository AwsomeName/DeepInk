# Research

Large upstream research checkouts do not live in the DeepInk desktop repository.

`agent-device` is consumed as the published npm package declared in
`DeepInk/package.json`. If a future investigation needs a source checkout, keep
it outside this repository or add it as an explicit submodule with a documented
purpose and lifecycle.
