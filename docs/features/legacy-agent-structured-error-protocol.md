# Archived Structured Runtime Error Protocol

The previous structured runtime-error protocol notes have been removed from the
Studio documentation set.

Studio OSS uses local execution errors for the built-in terminal and local
workbench. Official network runtime error contracts belong to the private
runtime and official overlay.

Current references:

- `docs/features/terminal-tab-model.md`
- `docs/commercial-overlay-interface-requirements.md`
