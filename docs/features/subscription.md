# Subscription

Subscription service documentation has moved to `../../../private-serv/docs/features/subscription.md`.

DeepInk keeps client UI and IPC integration only. Server-side plans, orders,
payments, and subscription state management belong to `private-serv`.
