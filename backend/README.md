# DeepInk Backend Stub

Server-side authentication and subscription code has moved to `../private-serv`.

DeepInk keeps only the desktop client integration here. Do not add backend
business logic, secrets, payment handlers, or cloud deployment scripts to this
directory.
