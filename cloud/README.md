# DeepInk Cloud Stub

Cloud functions and deployment artifacts have moved to `../private-serv/cloud`.

This directory is intentionally a placeholder so the desktop repository does
not carry closed-source service implementation code.
