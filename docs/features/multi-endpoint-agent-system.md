# Archived Multi-Endpoint Agent Plan

The previous multi-endpoint agent plan has been removed from the Studio
documentation set.

Studio OSS currently documents the local workbench, local Agent surface, local
tools, and extension boundaries. Official network endpoints and private runtime
targets belong outside the default open-source path.

Current references:

- `docs/features/agent-system.md`
- `docs/commercial-overlay-interface-requirements.md`
