# Archived Sync Plan

The previous sync plan has been removed from the Studio documentation set.

Studio OSS does not include a default cloud sync provider, account-bound sync,
or hosted storage backend. If official builds need sync, it must be injected by
the official overlay and keep secrets outside open-source defaults.

Current references:

- `docs/architecture.md`
- `docs/commercial-overlay-interface-requirements.md`
