# Archived Network Error Model

The previous network error model has been removed from the Studio documentation
set.

Studio OSS uses local execution and local IPC errors for built-in capabilities.
Official network-runtime error contracts belong to the private runtime and
official overlay.

Current references:

- `docs/features/terminal-tab-model.md`
- `docs/commercial-overlay-interface-requirements.md`
